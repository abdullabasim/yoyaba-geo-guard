# Browser-facing API base URL. Must be reachable from the user's machine.
NEXT_PUBLIC_API_URL=http://localhost:8100

# Server-side base URL used by React Server Components. Inside Docker this is
# the compose service name; when running `npm run dev` locally it is the same
# host URL as above.
INTERNAL_API_URL=http://localhost:8100
